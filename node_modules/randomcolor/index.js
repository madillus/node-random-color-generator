const randomColor = require('randomcolor');
const randomColorLibrary = randomColor.lib;
const command = process.argv[2];

// If the command exists in the library
if (randomColorLibrary[command]) {
  console.log(randomColorLibrary[command].char);
} else {
  console.log('Color not found');
}

const color = randomColor();
const hue = randomColor();
const saturation = randomColor();
const brightness = randomColor();

